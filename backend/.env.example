# Development mode setting
# Set to 'true' to use SQLite instead of Oracle
DEV_MODE=true

# Oracle Database Configuration
# These values are only used when DEV_MODE=false
DB_USER=username
DB_PASSWORD=password
DB_HOST=localhost
DB_PORT=1521
DB_SERVICE=xe

# Application Configuration
PORT=5000
FLASK_ENV=development 